<div class="problems-grid">
	<div class="problem-visual jpeg-white-bg">
		<img src="/demo-images/athena-white-bg.jpg" alt="Image avec fond blanc">
	</div>

	<div class="problem-visual object-fit-issue">
		<img src="/demo-images/achilles-stretched.png" alt="Image déformée">
	</div>

	<div class="problem-visual cls-issue">
		<img id="cls" src="" data-src="/demo-images/ares-cls.png" alt="Image qui déborde" loading="lazy">
	</div>
	<script>
		setTimeout(() => {
			const img = document.querySelector('#cls') as HTMLImageElement;
			img.src = img.dataset['src'];
		}, 500)
	</script>

		<div class="problem-visual broken-image">
			<img src="/demo-images/404.png" alt="Broken image">
		</div>

	<div class="problem-visual heavy-image">
		<img src="/demo-images/hermes-heavy.png" alt="Image lourde">
		<span class="weight-badge">11.5 MB</span>
	</div>

	<div class="problem-visual blurry-image">
		<img src="/demo-images/artemis-blurry.jpg" alt="Image floue">
	</div>
</div>

<style lang="scss">
	:global(body) {
		background-color: black;
	}
	:global(*) {
		box-sizing: border-box;
	}

	.problems-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		padding: 1rem;
		max-width: 85%;
		max-height: 85%;
		margin: 0 auto;
		align-items: center;
	}

	.problem-label {
		font-size: 0.9rem;
		text-align: center;
		margin: 0;
		line-height: 1.2;
	}

	// Styles spécifiques pour chaque problème
	.jpeg-white-bg {
		background: white;
		padding: 30px;
		aspect-ratio: 1 / 1;
		img {
			object-fit: cover;
			aspect-ratio: 1 / 1;
			width: 100%;
		}
	}

	.object-fit-issue {
		img {
			width: 100%;
			aspect-ratio: 4 / 1;
			object-fit: fill; // Déforme volontairement
		}
	}

	.cls-issue {
		img {
			width: auto;
			height: 600px; // Image plus grande que le conteneur
			object-fit: contain;
		}
	}

	.heavy-image {
		aspect-ratio: 1 / 1;
		width: 100%;
		img {
			aspect-ratio: 1 / 1;
			width: 100%;
			object-fit: contain;
		}
	}

	.blurry-image {
		img {
			filter: blur(4px);
		}
	}

	.broken-image {
		width: 100%;
		aspect-ratio: 1 / 1;
	}
</style>
